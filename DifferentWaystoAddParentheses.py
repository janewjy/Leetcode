def operation(op):
            return{
                '+':lambda x,y: x+y,
                '-':lambda x,y: x-y,
                '*':lambda x,y: x*y
            }[op]
import re

class Solution(object):
    def diffWaysToCompute(self, input):
        """
        :type input: str
        :rtype: List[int]
        """
        # tokens = re.split("(/D)",input)
        # nums = map(int,tokens[::2])
        # ops = tokens[1::2]
        if input.

        
